if XMLUI

if LARGEICONS
icons = connect_no.png connect_creating.png connect_established.png
else
icons =
endif

else
icons =
endif

actiondir   = $(kde_datadir)/konq-embed/actions/big
action_DATA = $(icons)
